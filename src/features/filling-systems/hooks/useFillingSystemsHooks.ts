import { useFillingSystem } from "./useFillingSystem";

// Re-export for easier access and to avoid import confusion
export const useFillingSystemsHooks = useFillingSystem;

// Default export for consistency with other hooks
export default useFillingSystem;
